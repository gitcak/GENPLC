/*
 * UI Components Implementation
 *
 * NOTE: Previously contained unused sprite-based drawing functions
 * (drawCard, drawCardCentered, drawKV, drawBar, lerp565).
 * Removed to reduce dead code. See components.h for details.
 *
 * Page-specific helpers now live directly in each page's .cpp file.
 */

#include "components.h"

namespace ui {

// All sprite-based card functions were removed as dead code.
// lerpColor() utility is available in theme.cpp if needed.

}
